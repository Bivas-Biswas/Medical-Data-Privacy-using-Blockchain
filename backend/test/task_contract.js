const MedicalReport = artifacts.require("MedicalReport");

contract("TaskContract", function () {
  it("should check tasks length is 1 after adding a task", async function () {
    // const MedicalReport = await MedicalReport.deployed()
    // await Task.addTask('walk the dog', false)
    // const tasks = await Task.getMyTasks()
    // const expected = 1
    // return assert.equal(tasks.length, expected, 'tasks length should be 1')
  });
});
